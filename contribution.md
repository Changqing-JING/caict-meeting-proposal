### environment setup

```shell
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### convert md to pdf

```shell
.venv/bin/python scripts/md_to_pdf.py
```

Output: `build/caict-meeting-proposal.pdf`
